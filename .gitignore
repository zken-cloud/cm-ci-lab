# CodeMender binary — sensitive (baked-in credential); never commit
cm-linux
container/cm-linux
# generated artifact variant
webapp/artifact.body.html
# local CM state / logs
**/.codemender/
*.log

# gated preview CLI binary + local validation scratch — never commit
preview-test/
